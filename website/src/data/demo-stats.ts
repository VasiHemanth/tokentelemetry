/**
 * Canonical demo dataset: 90 days of one machine's agent spend, matching the
 * dashboard screenshot in the hero. Single source of truth for every section
 * that quotes these figures (Ledger, BootShowcase, terminal script, hero).
 * The numbers are deterministic, not randomized — if you change one here,
 * check the baked-in copies in terminal-script.ts / BootShowcase / HowItWorks.
 */
export const DEMO_WINDOW_DAYS = 90;
export const DEMO_COST_USD = 599.85;
export const DEMO_TOKENS_MILLIONS = 931.8;
export const DEMO_SESSIONS = 510;
export const DEMO_AGENTS = 13;
