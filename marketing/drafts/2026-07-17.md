# Drafts — 2026-07-17 (Friday)

**Calendar row (Fri):** X = Feature announcement thread · Reddit = 2–3 helpful comments · Discord = changelog in #announcements
**Angle this week:** Money week (local-model cost/energy/savings)
**Source:** UPDATE.json release `2026-07-12` "Local model insights: energy, tok/s & savings" (newest user-facing ship). This week's other merges were fixes (PR #133 Copilot cache double-count, PR #131 Claude history integrity from @mariadb-KyleHutchinson) — no new features, so the thread leads with the 07-12 release.

> Before posting: the [bracketed numbers] are placeholders — drop in the real figures from your own Local Models page so nothing is invented.

---

## X / Twitter — Feature announcement thread

**Suggested media per tweet noted inline. Lead tweet carries the strongest visual.**

### Variant A (lead with the number)

**1/**
Ran a local model this week and wanted to know what it actually cost me in electricity — not a made-up per-token rate. So TokenTelemetry now measures it.

New: filter Analytics to local-only sessions and see watt-hours, electricity $, CO₂, tokens/sec, and savings vs cloud.
*attach: screenshot of Analytics filtered to local-only, energy + savings cards visible*

**2/**
There's a model efficiency table now too. It ranks your local models by throughput and Wh per 1M output tokens, so you can see which model is actually cheap to run — not just which one feels fast.
*attach: screenshot of the model efficiency table (/local-models)*

**3/**
Open any local session and it shows the run's generation time, energy used, electricity cost, and CO₂. Same forensic view you get for a cloud session, just priced by power instead of API rates.
*attach: screenshot of a single local session card with energy/cost/CO₂*

**4/**
It reads a live Ollama probe on loopback — installed + loaded models, plus NVIDIA GPU stats via nvidia-smi where present. macOS, Linux, Windows. Nothing leaves your machine, no remote host exposed.
*attach: short GIF of the Local Models page populating from the Ollama probe*

**5/**
All local, zero config, reads logs your agents already write. Repo's open:
github.com/VasiHemanth/tokentelemetry
#buildinpublic
*attach: (optional) the dashboard home with the Local & Power card*

### Variant B (shorter, 3 tweets)

**1/**
If you run models locally, "cost" isn't tokens — it's electricity. TokenTelemetry now prices local sessions by actual power draw: watt-hours, $ at your kWh rate, CO₂, and how much you saved vs running the same work on cloud.
*attach: screenshot of local-only Analytics with savings + energy cards*

**2/**
Plus a model efficiency table — throughput and Wh per 1M output tokens per local model — so you can pick the one that's genuinely cheap to run, and a live Ollama probe that reads your installed/loaded models (and GPU stats) right on the box.
*attach: screenshot of model efficiency table*

**3/**
No SDK, no cloud, no code changes. It reads the logs Ollama/llama.cpp already write.
github.com/VasiHemanth/tokentelemetry
#buildinpublic
*attach: GIF of Local Models page*

**Reply-engagement topics to search & jump into (X search):**
1. "ollama electricity cost" / "local LLM power draw" — people guessing at their run cost; offer the measured view.
2. "is running llama locally cheaper than API" — factual cost-comparison threads; share the savings-vs-cloud framing (no bashing).
3. "nvidia-smi tokens per second" / "local model tok/s benchmark" — folks comparing throughput; mention the efficiency table.

---

## Reddit — 2–3 helpful comments (no self-promo lead; value first)

Not a post day. Find threads where these are genuinely useful and answer plainly; mention TT only if directly relevant and asked-adjacent.

- **r/LocalLLaMA** — any "how much does running X locally actually cost in power?" thread: explain the Wh-per-1M-tokens math and that throughput matters as much as wattage. Link TT only if someone asks for a tool.
- **r/selfhosted** — "worth self-hosting an LLM vs paying API?" — give the honest tradeoff (electricity + hardware amortization vs per-token), no hype.
- **r/ollama** — questions about reading loaded models / GPU stats: helpful CLI answer first; TT's probe is a natural, non-pushy follow-up.

---

## Discord — changelog post in #announcements

**Draft:**

🔋 **Local model insights just landed** (v 2026-07-12)

If you run models locally, TokenTelemetry now tells you what they actually cost — in electricity, not fake per-token rates.

- **Local-only analytics** — filter to local sessions and see watt-hours, electricity $, CO₂, tokens/sec, and savings vs a reference cloud model.
- **Model efficiency table** — rank your local models by throughput and Wh per 1M output tokens. Open a local session for its gen time, energy, cost, and CO₂.
- **Live Ollama probe** — reads installed + loaded models on loopback, plus NVIDIA GPU stats via nvidia-smi. macOS / Linux / Windows, nothing exposed remotely.
- **Fixes** — SmallCode now stamps local billing + measures tok/s from wall-clock; Cline's Ollama runs price by electricity instead of cloud rates.

Also merged this week: Copilot CLI cache double-count fixed (#133), and full Claude session-history integrity + correct dating thanks to @mariadb-KyleHutchinson (#131).

All local, zero config. Update and check the Local Models page. 👉 /local-models
