# Drafts — 2026-07-24 (Friday)

**Calendar row (Fri):** X = Feature announcement thread · Reddit = 2–3 helpful comments · Discord = changelog in #announcements
**Angle this week:** Multi-agent week (one dashboard, every agent)

**Material check (honest):** `git log --since="7 days ago"` = **0 commits**. Nothing user-facing shipped this week. The newest UPDATE.json release is `2026-07-12` (local model insights) — but **last Friday's thread (07-17) already announced it**, so re-running it would be a stale repeat.

**Decision:** Per the calendar's "if nothing shipped, keep the heartbeat" rule — and the "never invent statistics / only cite real features" rule — I'm treating today's Feature-Friday slot as an **evergreen multi-agent spotlight** (all true, all shippable-now) plus an honest build-in-public variant. No fake PR counts, no invented roadmap. The one hard number used (42 agents) is from README.md.

> Before posting: swap any [bracketed] figure for a real one off your own dashboard so nothing is invented.

---

## X / Twitter — Feature-Friday slot (evergreen, multi-agent angle)

**Suggested media inline. Lead tweet carries the strongest visual.**

### Variant A — the unified-dashboard spotlight (thread, 4 tweets)

**1/**
I run more than one coding agent. Claude Code for the heavy stuff, Codex and Copilot depending on the repo, a local model when I don't want an API bill. The problem was never picking one — it was that each keeps its own logs and I had no single place to see what they all cost.

TokenTelemetry reads all 42 of them into one dashboard.
*attach: screenshot of the home dashboard with multiple agents' totals side by side*

**2/**
Nothing to instrument. No SDK, no API key, no code changes. It reads the session logs your agents already write to disk — Claude Code, Codex, Cursor, Copilot CLI, Gemini CLI, Grok Build, Antigravity, Hermes, and ~30 more — and shows tokens, cost, tool calls, and full traces per agent.
*attach: screenshot of the "All Agents" view / agent filter*

**3/**
Because it's one view, you can actually compare: which agent burned the most tokens this week, which model each one defaulted to, which project drove the spend. Filter by agent, model, or project and the whole dashboard re-slices.
*attach: screenshot of Analytics filtered to 2–3 agents, model distribution visible*

**4/**
All local. Your logs never leave the machine. Zero config — it auto-detects agents from their default log locations.
github.com/VasiHemanth/tokentelemetry
#buildinpublic
*attach: short GIF of switching the agent filter and the numbers updating*

### Variant B — honest build-in-public (single post + optional 2nd)

**1/**
No new feature to announce this Friday — quiet week on my end, and I'd rather not manufacture a changelog just to keep a streak.

So instead: the thing TokenTelemetry already does that I still think is underrated — it prices *local* models by electricity and *cloud* agents by real token rates, in the same dashboard, with zero setup.
*attach: screenshot showing a local session ($ = watt-hours) next to a cloud session (($ = API rate) in the same list*

**2/** (optional)
No SDK, no account, no code changes — it reads the logs 42 agents already write. If you run more than one agent, that's the whole pitch.
github.com/VasiHemanth/tokentelemetry
*attach: home dashboard screenshot*

> Pick A if you want the clean product thread; pick B if you'd rather be transparent that it was a slow week (that honesty tends to land well with the #buildinpublic crowd).

**Reply-engagement topics to search & jump into (X search):**
1. "which AI coding agent is cheapest" / "Claude Code vs Codex cost" — cost-comparison threads; offer the unified-view framing, factual, no bashing.
2. "juggling multiple coding agents" / "switched from Cursor to Claude Code" — people running 2+ agents; mention the single dashboard, don't hard-sell.
3. "Langfuse / LangSmith for coding agents" — folks reaching for SDK-based observability on CLI agents; note the no-instrumentation, log-reading difference as a factual alternative.

---

## Reddit — 2–3 helpful comments (value first, no self-promo lead)

Not a post day. Find threads where these are genuinely useful; mention TT only if directly relevant and asked-adjacent.

- **r/ClaudeAI / r/ChatGPTCoding** — any "how do I know what my agent runs are costing me?" thread: explain that the agents already log tokens locally and you can read them without an account. Link TT only if someone asks for a tool.
- **r/LocalLLaMA** — "is it cheaper to run locally than pay API?" — give the honest electricity-vs-per-token tradeoff; TT is a natural follow-up if they want it measured, not asserted.
- **r/selfhosted** — "self-hosted dashboard for AI usage" type asks: helpful answer first, then TT as the local, zero-cloud option — one line, no pitch.

---

## Discord — #announcements (no changelog this week — community note instead)

**Draft:**

Quiet week on shipping — no new features merged, so no changelog today. 🙂

Everything from the **07-12 local-model insights** drop is stable (energy, tok/s, savings, live Ollama probe), and the Claude session-history + Copilot cache fixes from the week before are holding up. If you've hit anything odd since updating, drop it in #bugs — genuinely the most useful thing you can post right now.

Back to features next week. If there's an agent you want tracked that isn't in the 42 yet, reply here or open an issue 👉 github.com/VasiHemanth/tokentelemetry/issues

---

### Reviewer note (30-sec version)
- **X:** pick Variant A (product) **or** B (honest slow-week). Both are true; B is more #buildinpublic.
- **Reddit:** comments only today.
- **Discord:** honest "no ship this week" note — or skip entirely if you'd rather stay silent on a quiet week.
- Nothing here invents a stat. Only real number = 42 agents (README).
