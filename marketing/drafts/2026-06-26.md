# Content Drafts — Friday, June 26 2026

> Source: UPDATE.json tag 2026-06-23 · git log since 2026-06-23
> Week's features: live process monitor, concurrency timeline, attribution cost tree, workflow grouping + UI
> Full X thread + LinkedIn + Discord drafts also in: `feature-friday-2026-06-26.md`

---

## X / Twitter — Feature Friday Thread

**Tweet 1 — Hook**

Four new multi-agent features shipped in TokenTelemetry this week. If you're running more than one AI coding agent at a time, this is the update you've been waiting for. 🧵

*attach: screenshot of /workflows page showing a named task with aggregated cost + multiple agents*

---

**Tweet 2 — Workflow grouping**

New: Workflows. Group sessions from any agent into a named task and see the total cost, tokens, and which agents are still active.

Used Claude Code for planning, Codex for implementation, another agent for review? Tag them all. One number at the end.

*attach: /workflows page showing a named workflow with 3 agent sessions, total cost callout*

---

**Tweet 3 — Concurrency timeline**

New: concurrency timeline shows exactly when your agents overlapped — and what the combined $/hr was during those windows.

Running parallel agents isn't just "double the cost." Now you can see when the peak burn actually happened.

*attach: concurrency timeline showing two overlapping session bars with combined cost/hr*

---

**Tweet 4 — Attribution cost tree**

New: when a parent agent spawns subagents, the parent session now rolls up the full cost of the entire delegation tree.

Previously: you saw only the parent's own tokens. The subagent cost was invisible.

Open any parent session to see the recursive breakdown.

*attach: session detail page showing parent + delegation tree with per-child token/cost rows*

---

**Tweet 5 — Live process monitor**

New: a System panel shows real-time CPU, memory, and disk IO for every AI agent process on your machine.

Write rate > 5 MB/s triggers a warning banner. This is the exact class of issue that caused Codex Desktop to write ~222 TB/year in the background — unnoticed.

*attach: System panel screenshot showing agent processes + disk IO + warning banner*

---

**Tweet 6 — Close**

All local. Zero config. Free. Open source. No account, no cloud, no data leaves your machine.

```
curl -fsSL https://raw.githubusercontent.com/VasiHemanth/tokentelemetry/main/install.sh | bash
```

→ https://github.com/VasiHemanth/tokentelemetry

---

**3 reply-engagement topics to search + engage with on X today:**

1. Search: `"running multiple agents"` or `"parallel claude code"` — people asking how to manage concurrent agent costs
2. Search: `"codex disk usage"` or `"claude code storage"` — the 222 TB disk-write conversation is still active
3. Search: `"agent observability"` or `"llm tracing local"` — compare-and-contrast conversations where TT's no-cloud angle is the differentiator

---

## Reddit — 2–3 Helpful Comments

*(Find relevant threads in r/ClaudeAI, r/ChatGPTCoding, r/LocalLLaMA, or r/selfhosted. Drop one of these where it fits naturally — don't force it.)*

**Comment A — for "how do I know what my agents are costing me?" threads**

One option that works without any SDK changes: TokenTelemetry (https://github.com/VasiHemanth/tokentelemetry). It reads the logs your agents already write — Claude Code, Codex, Cursor, Ollama, etc. — and puts cost and token usage in a local dashboard. Nothing leaves your machine. This week they added workflow grouping so you can tag sessions from multiple agents into one task and see the combined total.

**Comment B — for "does anyone monitor AI agent resource usage?" threads**

If the concern is disk IO / memory: TokenTelemetry just added a live process monitor panel that shows CPU, memory, and disk write rates per agent process in real time. It flags anything above 5 MB/s write rate, which is the same class of runaway that caused Codex Desktop to log ~222 TB/year. Local-only, no account, installs in one line.

**Comment C — for "how to see costs across multiple agents" threads**

TokenTelemetry (open source, local dashboard) now has a concurrency timeline — shows which agent sessions overlapped and what the combined hourly cost was during those windows. If you run Claude Code + Codex in parallel it shows you the actual simultaneous burn rate. There's also a new Workflows feature for tagging cross-agent sessions into a single task.

---

## Discord #announcements

**TokenTelemetry — Feature Friday (2026-06-26)**

Four features shipped this week, all focused on multi-agent visibility:

**🗂 Workflow grouping** — tag sessions from any agent into a named task. See combined cost, tokens, and active agents in one view. Grid/list/compact toggle + search/filter included.

**🔀 Concurrency timeline** — see which sessions ran at the same time and the combined $/hr during those overlaps.

**🌳 Recursive cost attribution** — parent sessions now roll up the full cost of their entire subagent delegation tree, recursively.

**📊 Live process monitor** — System panel shows real-time CPU, memory, and disk IO per agent process. 5 MB/s write threshold triggers a warning banner.

Update:
```
curl -fsSL https://raw.githubusercontent.com/VasiHemanth/tokentelemetry/main/install.sh | bash
```

Full changelog: https://github.com/VasiHemanth/tokentelemetry/commits/main
