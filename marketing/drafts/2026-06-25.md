# Content Drafts — Thursday, June 25, 2026

**Today's slots:** X reply-day · Reddit comments · LinkedIn milestone post

---

## X / Twitter — Reply Day (no original post)

No new post today. Spend ~20 min finding 5–8 conversations to reply to. Suggested searches:

1. `"Claude Code tokens"` — people surprised by their token bill
2. `"AI agent cost"` OR `"coding agent expensive"` — frustration threads
3. `"Codex pricing"` OR `"Codex usage"` — OpenAI Codex users checking costs
4. `"observability LLM"` OR `"LLM tracing"` — devs evaluating Langfuse/LangSmith
5. `"Ollama cost"` OR `"local LLM cost"` — people trying to quantify local model savings

**Reply angle:** concrete, not promotional. If they're asking "how do I see what I'm spending on Claude Code?" — answer directly (tell them TT reads the JSONL logs, no config needed), then link if relevant. Don't open with "check out my tool."

---

## Reddit — Helpful Comments (2–3 threads)

Find threads in r/ClaudeAI, r/LocalLLaMA, r/ChatGPTCoding, or r/selfhosted where people ask about tracking agent spend, token usage, or why their bill is high. Drop one of these adapted comments:

---

**Template A — token spend question**

> If you're on Claude Code, all your usage is already logged in `~/.claude/projects/` as JSONL. You can grep it manually, but [TokenTelemetry](https://github.com/VasiHemanth/tokentelemetry) reads it automatically and shows a dashboard — total tokens, cost by session, which models you used. No signup, no SDK, no code changes — it just reads the files the agent already wrote. `npx tokentelemetry` and it's running in 30 seconds.

---

**Template B — multi-agent / parallel agent thread**

> One thing that's easy to miss with parallel agents: the costs compound fast and you often can't tell which agent is the expensive one. The concurrency timeline in TokenTelemetry shows which sessions were running at the same time and the combined $/hour during overlaps — useful if you're running Cursor + Claude Code simultaneously and want to see the actual burn rate.

---

**Template C — "Codex writing too much to disk" angle (if a relevant thread exists)**

> There was a notable case where Codex Desktop was writing ~222 TB/year in disk IO before anyone noticed. TokenTelemetry added a live process monitor for exactly this: it shows real-time CPU, memory, and disk IO per agent process, and flags write rates above 5 MB/s. Worth running if you have multiple agents active at once.

---

## LinkedIn — Milestone / Community Post

**Variant A** *(if you have a recent star/contributor milestone)*

---

Big week for TokenTelemetry.

Four features shipped on Monday that I think change how you actually think about multi-agent costs:

**Live process monitor** — real-time CPU, memory, and disk IO per agent process, with a warning when write rates spike. (This is the feature I wish had existed when Codex Desktop was quietly writing 222 TB/year to disk.)

**Concurrency timeline** — see which agent sessions overlapped and the combined $/hour during those windows. Running Claude Code and Cursor in parallel looks very different on a timeline than in a sessions list.

**Attribution cost tree** — when a parent agent spawns subagents, you now see the full delegation cost, not just the parent's own tokens. For agentic workflows, that number is often 3–5× what the top-level session shows.

**Workflow grouping** — tag sessions from any agent into a named workflow (e.g. "fix auth bug") and see total cost across everything you used for that task.

All of this is local. No cloud, no SDK, no code changes — TokenTelemetry reads the logs your agents already write.

If you're doing serious work with AI coding agents, you probably want this running: github.com/VasiHemanth/tokentelemetry

attach: screenshot of the concurrency timeline or workflow grouping view

---

**Variant B** *(shorter, more conversational)*

---

Something I noticed building with multiple AI agents every day: the costs only make sense in aggregate.

One Claude Code session: $0.40, fine. Three agents running in parallel for 2 hours on the same feature: the number looks very different when you see the combined $/hour on a timeline.

Shipped this in TokenTelemetry this week — a concurrency timeline that shows overlapping sessions and their combined burn rate. Also added workflow grouping so you can tag everything you used for one task (regardless of which agent) and see the real total.

It's open-source, 100% local, zero config. npx tokentelemetry and you're looking at your data in 30 seconds.

github.com/VasiHemanth/tokentelemetry

attach: concurrency timeline screenshot

---
