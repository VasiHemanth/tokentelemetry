# Drafts — Friday 2026-08-14

> **Calendar row (Fri):** X = Feature announcement thread · Reddit = 2–3 helpful comments · Discord = changelog in #announcements. (LinkedIn/Dev.to have no Friday slot; not Wednesday, so no article outline / Reddit value-post.)
> **Rotation angle this week:** multi-agent (new agent coverage) + cost honesty.
> **Sourcing:** built from *verified* `origin/main` commits (HEAD `ba9c970`, 2026-08-13). NOTE: the repo is checked out on `feat/local-model-insights` (HEAD stuck at an Aug 6 commit), so `git log` on the working branch looks empty — the real week's work is on `origin/main`. `UPDATE.json` is **stale** (newest entry 2026-07-12); please add an entry for this week.

## ⚠️ Read before posting
- **Everything below maps to a real merged commit** (hashes in the maintainer note per item). No invented numbers — I did **not** state a total agent count; add one yourself only after confirming it live.
- **Muse & Prime are genuinely tracked now** — PR #253 (`5de560c feat: ingest Muse and Prime agent sessions`) adds backend detection + tests + icons. Confirm their names/branding are OK to announce publicly before posting.
- **`d14894a` (session-trace column fix) is NOT on main** — don't announce it; it's on a working branch. Save for next week.
- **tomaioo security fix (PR #243) merged Aug 5** — prior week, not strictly "this week." Kept as a light Discord credit; drop if you'd rather keep the post to this-week-only.
- A longer variant already exists at `drafts/feature-friday-2026-08-14.md` from yesterday's run — this file is the tighter, re-verified version. Pick one.

---

## X / Twitter — Feature announcement thread

**Verified against:** Muse/Prime `5de560c` (PR #253) · Hermes explorer `618c0d0`+`286bc8d` (PR #258) · unpriced disclosure `be2f218` · cache reads `6e85da5`,`b6b940c`.

**Tweet 1 — hook**
Two more coding agents show up on your TokenTelemetry dashboard this week: Muse and Prime.

If you run them, their sessions — model, tokens, cost, working directory, full trace — now sit next to Claude Code, Codex, Cursor and the rest. One local dashboard, no config.

🧵
*[attach: dashboard screenshot with the Muse + Prime agent cards visible among the existing agents]*

**Tweet 2 — how it works**
Nothing to wire up. If the sessions are on disk, they get read:
- recorded working directories map into project/worktree nav, so sessions land under the right repo
- Muse subagent attribution is preserved — delegated work stays traceable
- Prime uses its own session branch + reported cost
*[attach: a Muse session trace with the project/worktree breadcrumb]*

**Tweet 3 — cost honesty**
Also shipped: we stopped showing cost numbers we hadn't earned.
- cache-read tokens now priced cumulatively for Claude, Qwen and Cursor (was undercounting)
- where TokenTelemetry can count Claude activity but can't price it locally, it now says so instead of dropping it from totals
*[attach: analytics view showing the "counted but unpriced" disclosure]*

**Tweet 4 — Hermes explorer**
If you run Hermes: full session history moved into a dedicated, URL-filterable explorer. It's a load-more list, not "Page 7 of 12" — one request returns the whole visible set, so a stale page number can't strand you on an empty screen. Filter, wander off, come back — filters persist.
*[attach: 15–20s GIF filtering the explorer, hitting Load more, navigating away and back with filters intact]*

**Tweet 5 — close**
Free, open source, 100% local — your logs never leave your machine.

```
curl -fsSL https://raw.githubusercontent.com/VasiHemanth/tokentelemetry/main/install.sh | bash
```
→ github.com/VasiHemanth/tokentelemetry

**Alt Tweet 1 (single-post variant, if you skip the thread):**
TokenTelemetry now tracks two more coding agents — Muse and Prime — alongside Claude Code, Codex, Cursor and the rest. Sessions, tokens, cost, full trace, zero config: if it's on disk, it shows up. Plus more honest cost math on cache reads. 100% local.
*[attach: dashboard screenshot showing the new agents]*

**3 reply-engagement topics to search for on X today:**
1. People comparing what Claude Code / Cursor / Codex *actually cost* them per month — offer the local, no-SDK angle.
2. "How do I track token usage across multiple agents" / multi-agent tooling threads.
3. Cache-read / prompt-caching billing confusion (people surprised by cache token costs) — the cumulative-pricing fix is a concrete, non-salesy reply.

---

## Reddit — 2–3 helpful comments (no self-promo unless directly asked)

Rotate where you already have karma; lead with help, mention TT only if it answers the question.
1. In a thread about surprise agent bills: explain how cache-read tokens get undercounted and how to actually see cumulative cost; mention TT only if someone asks for a tool.
2. In a multi-agent / "which agent is cheapest" thread: describe the passive-log-reading approach (no SDK, no code change) as one option among others.
3. In a self-hosted/local-first thread: answer a real question about keeping agent telemetry off the cloud.

---

## Discord — #announcements changelog

**TokenTelemetry — Feature Friday (2026-08-14)**

Agent coverage + cost honesty this week:

- **🆕 Two new agents: Muse & Prime.** Their sessions — model, tokens, cost, working directory, full trace — now show up alongside every agent you already track. Working directories map into project/worktree nav, Muse subagent attribution is preserved, Prime uses its own session branch + reported cost. Zero config. *(PR #253)*
- **🔎 New Hermes session explorer.** Full history moved to a dedicated, URL-filterable explorer. Load-more list instead of pagination — one request returns the whole visible set, so no stale page number stranding you on an empty screen — and filters now survive a trip through the sidebar and back. *(PR #258)*
- **💵 Truer cache-read billing.** Cache-read tokens are now priced cumulatively for Claude, Qwen and Cursor (was undercounting), and Analytics now discloses Claude activity it can count but can't price locally instead of silently dropping it from totals.
- **🔒 Security (landed last week):** the Antigravity summarizer no longer runs `agy` with `--dangerously-skip-permissions`. Thanks to **tomaioo** for reporting and fixing this (PR #243).

Install or update:
```
curl -fsSL https://raw.githubusercontent.com/VasiHemanth/tokentelemetry/main/install.sh | bash
```
Full changelog: https://github.com/VasiHemanth/tokentelemetry/commits/main
